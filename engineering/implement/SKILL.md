---
name: implement
description: コード、設定、テスト、schema、依存関係、agent 指示を作成または変更する実装タスクを、必要な専門 skill を使い、検証と完成差分のレビューまで完了させる。調査、診断、計画、レビューだけの依頼、文書だけの編集、commit、push、PR 作成だけの依頼では使わない。
---

# 実装

依頼された成果を実装し、必要な検証と完成差分の確認まで終える。
目標・対象範囲・完了条件を基準に進め、無関係な改善を加えない。

## 設計と検証

公開 API・型・schema の契約を明確にする。静的に検査できる規則は linter や formatter に置く。
暫定実装の TODO には追跡先と解消条件を残す。
成果物に合う検証で完了条件を確かめ、指定された KPI やカバレッジ目標は達成まで取り組む。

必要な専門 skill を使う。

- 実行可能な振る舞いの変更：`tdd`
- テストの追加・変更：`test-writing-style`
- agent skill の作成・改善：`skill-workbench`
- agent 向け prompt の改善：`improve-agent-prompt`
- 検証済みコードの簡素化：`simplify-code`

## レビュー範囲の選択

完成差分を要求・退行・検証不足の観点で確認する。
skill のレビューは `skill-workbench` を使う。
その他は変更リスクに見合う範囲で行い、重大な影響または検証の不足がある場合は `review-diff-code` による独立レビューを加える。
修正後は影響範囲を再確認する。

## 完了

依頼された成果が成立し、必要な検証・レビューが成功し、採用した指摘が解消されていれば完了する。
成果、検証結果、残る制約を報告する。必須の確認ができない範囲は未完了として示す。
